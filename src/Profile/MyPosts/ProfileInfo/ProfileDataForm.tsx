import React from "react";
import {ProfileDataPropsType} from "./ProfileData";

export const ProfileDataForm: React.FC<ProfileDataPropsType> = ({profile}) => {
    return <div>
        Form
    </div>
}